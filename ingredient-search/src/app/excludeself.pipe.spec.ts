import { ExcludeselfPipe } from './excludeself.pipe';

describe('ExcludeselfPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeselfPipe();
    expect(pipe).toBeTruthy();
  });
});
